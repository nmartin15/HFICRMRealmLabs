app = "realm-labs-crm-backup"
primary_region = "sjc"

[build]
  dockerfile = "Dockerfile.backup"

[env]
  TZ = "America/Los_Angeles"
  BACKUP_RETENTION_DAYS = "30"

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1
